// Style modal
$('#dialog .modal-content').addClass('panel-danger');

// Add the dialog title
$('#dialog h4').html("<i class=' glyphicon glyphicon-trash'></i>  Eliminar la plantilla <%= @external_order_template.name %>");

// Render the edit form
$('#dialog .modal-body').html(
  'Desea eliminar esta plantilla de <%= @external_order_template.external_order_template_supplies.count %> insumos?');

// Add form button
$('#dialog .modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<a data-remote='true' data-method='delete' href='/external_order_templates/<%= @external_order_template.id %>' data-dismiss='modal' class='btn commit btn-danger pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Remove style
$('#dialog').on('hidden.bs.modal', function () {
  $('#dialog .modal-content').removeClass('panel-danger');
});
