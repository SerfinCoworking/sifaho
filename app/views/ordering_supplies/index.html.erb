<%= render 'header' %>

<% if @ordering_supplies.present? %>
  <%= render(
    partial: 'ordering_supplies/list',
    locals: { ordering_supplies: @ordering_supplies }
  ) %>
<% else %>
  <%= render(
    partial: 'shared/not_found',
    locals: { object_name: 'pedidos despachados' }
  ) %>
<% end %>
