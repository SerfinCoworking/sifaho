<%= render 'header' %>

<% if @applicant_orders.present? %>
  <%= render(
    partial: 'ordering_supplies/applicant_list',
    locals: { applicant_orders: @applicant_orders }
  ) %>
<% else %>
  <%= render(
    partial: 'shared/not_found',
    locals: { object_name: 'pedidos recibidos' }
  ) %>
<% end %>