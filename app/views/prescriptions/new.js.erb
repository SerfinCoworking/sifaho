// Add the dialog title
$('#dialog h4').html("<i class='glyphicon glyphicon-plus'></i>  Nueva prescripción");

// Render the new form
$('.modal-body').html('<%= j render("form") %>');

// Add form button
$('.modal-footer').html(
  "<div class='text-center'>"+
    "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
    "<button type='submit' name='commit' class='btn btn-primary' form='new_prescription' value='Cargar prescripción'>Cargar</button>"+
    "<button type='submit' name='commit' class='btn btn-success pull-right' form='new_prescription' value='Cargar y dispensar'>Cargar y dispensar</button>"+
  "</div>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
  $('#prof_enrollment').focus();
});

<%= render 'form_function' %>
