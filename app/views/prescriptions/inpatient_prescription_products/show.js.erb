$("#<%= @tr_id %>").find('.product-code').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.product.code %>");

$("#<%= @tr_id %>").find('.product-id').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.product.id %>");

$("#<%= @tr_id %>").find('.product-name').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.product.name %>");

$("#<%= @tr_id %>").find('.stock-quantity').
                    first().
                    removeClass("is-invalid").
                    val("<%= current_user.sector.stock_to(@inpatient_prescription_product.product) %>");

$("#<%= @tr_id %>").find('.product-deliver-quantity').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.deliver_quantity %>");

$("#<%= @tr_id %>").find('.product-dose-total').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.total_dose %>");

$("#<%= @tr_id %>").find('.product-observartion').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.observation %>");


$("#<%= @tr_id %>").find('.dose-interval').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.interval %>");

$("#<%= @tr_id %>").find('.dose-quantity').
                    first().
                    removeClass("is-invalid").
                    val("<%= @inpatient_prescription_product.dose_quantity %>");

  