<% content_for :page_header do %>
  <li class="nav-item <%= active_action('index') %> non-sel" id="page-title">
    <%= link_to reports_path, class: 'nav-link' do %>
      <%= content_tag(:span, '', class: 'glyphicon glyphicon-list-alt') %>
      Reportes
    <% end %>
  </li>
  <li class="nav-item <%= active_action('new_delivered_by_order') %> non-sel">
    <%= link_to new_delivered_by_order_reports_path, class: 'nav-link' do %>
      <%= content_tag(:span, '', class: 'glyphicon glyphicon-tint') %>
      <span class="hidden-xs"> Entregado por pedido</span>
    <% end %>
  </li>
  <li class="nav-item <%= active_action('new_delivered_by_establishment') %> non-sel">
    <%= link_to new_delivered_by_establishment_reports_path, class: 'nav-link' do %>
      <%= content_tag(:span, '', class: 'glyphicon glyphicon-tint') %>
      <span class="hidden-xs"> Entregado por establecimiento</span>
    <% end %>
  </li>
<% end %>