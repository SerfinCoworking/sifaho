// Style modal
$('.modal-header').addClass('bg-danger text-white');

// Add the dialog title
$('.modal-title').html("<i class='fa fa-trash'></i>  Eliminar recibo");

// Render the edit form
$('.modal-body').html('Eliminar recibo de <%= @receipt.applicant_sector.sector_and_establishment %>?');

// Add form button
$('.modal-footer').html(
  "<a class='btn' data-dismiss='modal'>Cancelar</a>"+
  "<a data-remote='true' data-method='delete' href='/receipts/<%= @receipt.id %>>' data-dismiss='modal' class='btn commit btn-danger pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Remove style
$('#dialog').on('hidden.bs.modal', function () {
  $('.modal-header').removeClass('bg-danger text-white');
});
