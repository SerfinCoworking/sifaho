 <%= simple_form_for 'remote_form', remote: true, url: permissions_users_admin_path(@user), method: 'GET', html: { novalidate: true} do |f| %>
  <div class="form-inputs">
      <%= f.select( :sector, 
        @user.sectors.all.map { 
          |sector| ["#{sector.name} - #{sector.establishment.name}", sector.id] 
        },
        { include_blank: false, include_hidden: false, selected: (@sector.id if @sector.present?) },
        { 
          class: "selectpicker-md show-tick custom-select-pick",
          allow_blank: false,
          title: 'Seleccionar sector',
          id: "remote_form_sector",
          "data-width"=>"100%",
          "data-live-search"=>"true",
          "data-live-search-normalize"=>"true"
        }
      ) %>
  </div>
<% end %>