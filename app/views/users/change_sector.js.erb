// Style modal
$('#dialog .modal-header').addClass('bg-primary text-white');

// Add the dialog title
$('#dialog .modal-title').html("<i class='fa fa-street-view'></i>  Cambiar ubicación");

// Render the new form
$('#dialog .modal-body').html('<%= j render("change_form") %>');

// Add form button
$('#dialog .modal-footer').html(
  "<button type='button' class='btn btn-secondary' data-dismiss='modal'>Volver</button>"+
  "<button type='submit' name='commit' class='btn btn-success pull-right' form='change_form'><i class='fa fa-exchange-alt'></i> Cambiar</button>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
  $('.first_input').focus();
});

// Remove style
$('#dialog').on('hidden.bs.modal', function () {
  $('#dialog .modal-header').removeClass('bg-primary text-white');
});

$('.selectpicker').selectpicker(); // Se inicializa selectpicker
