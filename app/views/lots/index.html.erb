<% render 'header' %>

<div class="card fixed-custom-card">
  <div class="card-header">
    <%= render 'filter' %>
  </div>
  <div class="card-body fixed-panel-index">
    <%= render(
      partial: 'lots/list',
      locals: { lots: @lots }
    ) %>
  </div>
</div>
