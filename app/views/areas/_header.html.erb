<% content_for :title, "Rubros" %>

<% content_for :page_header do %>
  <li class="nav-item non-sel" id="page-title">
    <%= link_to areas_path, class: "nav-link #{ active_action('index') }" do %>
      <%= fa_icon "sitemap" %>
      Rubros
    <% end %>
  </li>
  <% if policy(Area).new? %>
    <li class="nav-item non-sel">
      <%= link_to new_area_path, class: "nav-link #{ active_action('new') }" do %>
        <%= fa_icon "plus" %>
        Agregar rubro
      <% end %>
    </li>
  <% end %>
<% end %>