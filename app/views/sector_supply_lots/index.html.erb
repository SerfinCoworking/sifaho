<% content_for :page_header do %>
  <h3>Stock <%= current_user.sector.sector_name %>
    <%= button_tag(class: "btn btn-info btn-sm", 'data-toggle' => 'collapse', 'data-target' => '#filter') do %>
      <%= content_tag(:span, '', class: 'glyphicon glyphicon-filter') %>
      Filtrar
    <% end %>
  </h3>
<% end %>

<%= render "filter" %>

<%= render(
  partial: 'sector_supply_lots/list',
  locals: { sector_supply_lots: @sector_supply_lots }
) %>
