<% content_for :page_header do %>
  Stock <%= current_user.sector.name %>
  <% if policy(OrderingSupply).new_receipt? %>
    <%= link_to new_receipt_ordering_supplies_path, class: 'btn btn-primary btn-xs' do %>
      <%= content_tag(:span, '', class: 'glyphicon glyphicon-shopping-cart') %>
      Cargar stock
    <% end %>
  <% end %>
<% end %>

<%= render "filter" %>

<%= render(
  partial: 'sector_supply_lots/list',
  locals: { sector_supply_lots: @sector_supply_lots }
) %>
