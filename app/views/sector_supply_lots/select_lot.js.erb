// Add the dialog title
$('.modal-dialog').addClass('modal-lg');
$('.modal-header').addClass('bg-secondary text-white');
$('.modal-title').html("<i class='fa fa-barcode'></i>  Seleccionar lote");

// Render the edit form
$('.modal-body').html('<%= j render("lot_list") %>');

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn' data-dismiss='modal'>Volver</button>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Remove style
$('#dialog').on('hidden.bs.modal', function () {
  $('.modal-header').removeClass('bg-secondary text-white');
});
