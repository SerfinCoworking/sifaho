// Add the dialog title
$('#dialog h3').html("<i class=' glyphicon glyphicon-trash'></i>  Borrando profesional");

// Render the edit form
$('.modal-body').html('Esta seguro que desea eliminar al profesional <%= @professional.fullname %>?');

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<a data-remote='true' data-method='delete' href='/professionals/<%= @professional.id %>>' data-dismiss='modal' class='btn commit btn-danger pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");
