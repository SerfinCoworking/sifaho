<%= render 'header' %>

<div class="card">
  <div class="card-header bg-warning">
    <h5 class="card-title mb-0">
      <%= fa_icon "edit" %> Editando médico
      <%= link_to :back, class: 'btn float-right p-0',
        :title => 'Cerrar', :'data-placement'=>'top', :'data-toggle'=>'tooltip' do %>
        <%= fa_icon "times" %>
      <% end %>
    </h5>
  </div>
  <div class="card-body" id="panel-body-new">
    <%= render 'form', professional: @professional %>
  </div>
  <div class="card-footer text-right">
    <%= link_to 'Volver', professionals_path, class: 'btn' %>
    <button type='submit' name='commit' class='btn btn-success' form='edit_professional_<%=@professional.id%>'>
      <%= fa_icon "save" %> Guardar cambios
    </button>
  </div>
</div>
