$('#profile_date_of_birth').datetimepicker({format: 'DD/MM/YYYY'});

$('.chosen-select').chosen({
    allow_single_deselect: true,
    no_results_text: 'No se encontró el resultado',
    width: '270px'});

<% if @profile.date_of_birth.present? %>
  $('#profile_date_of_birth').val("<%= @profile.date_of_birth.strftime("%d/%m/%Y") %>");
<% end %>
