<nav id="custom-bootstrap-menu" class="navbar navbar-default navbar-default-cust navbar-fixed-top">
  <div class="container-fullwidth">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to main_app.root_path, data: { turbolinks: false }, class: 'navbar-brand navbar-brand-cust navbar-left' do %>
        <%= image_tag('sifaho-brand.png')%>
      <% end %>
      <% if user_signed_in? %>
        <% if current_user.sector.present? %>
          <div class="userinfo"> 
            <span><%= current_user.sector.name.upcase %></span>
            <br>
            <span><small><%= current_user.establishment_name %></small></span>
          </div>
        <% else %>
          <div class="brandinfo"> 
            Sistema Farmacéutico Hospitalario
          </div>
        <% end %>
      <% else %>
        <div class="brandinfo"> 
          Sistema Farmacéutico Hospitalario
        </div>
      <% end %>
    </div>
    <div class="">
      <ul class="nav nav-cust navbar-nav navbar-nav-cust navbar-right hidden-xs">
        <%= render 'devise/menu/login_items' %>
      </ul>
      <ul class="nav nav-cust navbar-nav navbar-nav-cust navbar-right hidden-xs">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
