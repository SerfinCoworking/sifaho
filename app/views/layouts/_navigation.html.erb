<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
 <!--Brand-->
  <div class="brand-container d-flex">
    <% if policy(:dashboard).sidebar? %>
      <button class="btn btn-light" id="menu-toggle"><%= fa_icon "bars" %></button>
    <% end %>
    <%= link_to main_app.root_path, data: { turbolinks: false }, class: "navbar-brand #{active_class('welcome')}" do %>
      <%= image_tag("sifaho-brand.png", size: "150x47", alt: "Logo SIFAHO", class: 'logo') %>
    <% end %>
  </div>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto module-links-navbar">
      <%= yield :page_header %>
    </ul>
    <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
      <%= render 'layouts/navigation_links' %>
      <%= render 'layouts/login_items' %>
    </ul>
  </div>
</nav>
