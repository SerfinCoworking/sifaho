// Add the dialog title
$('#dialog h4').html("<h4><i class=' glyphicon glyphicon-repeat'></i>  Restaurando insumo</h4>");

// Render the edit form
$('.modal-body').html(
  'Esta seguro que desea restaurar el siguiente insumo?<br>'+
  "<strong>Código: </strong><%= @supply.id %><br>"+
  "<strong>Insumo: </strong><%= @supply.name %><br>"+
  "<strong>Eliminado: </strong><%= @supply.deleted_at.strftime('%d/%m/%y %H:%M') %>"

);

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<a data-remote='true' data-method='get' href='/supplies/<%= @supply.id %>/restore' data-dismiss='modal' class='btn commit btn-primary pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");
