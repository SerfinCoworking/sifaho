// Add the dialog title
$('.modal-header').addClass("bg-danger text-white");
$('.modal-title').html("<i class='fas fa-trash'></i>  Borrando insumo");

// Render the edit form
$('.modal-body').html('Esta seguro que desea eliminar el insumo <%= @supply.name %>?');

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<a data-remote='true' data-method='delete' href='/supplies/<%= @supply.id %>>' data-dismiss='modal' class='btn commit btn-danger pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

$('#dialog').on('hidden.bs.modal', function () {
  $('.modal-header').removeClass('bg-danger text-white');
});
