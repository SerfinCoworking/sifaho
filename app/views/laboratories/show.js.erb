// Add the dialog title
$('#dialog .modal-title').html("<i class='fa fa-eye'></i> Viendo laboratorio");

// Render the edit form
$('#dialog .modal-body').html('<%= j render("show") %>');

// Add form button
$('#dialog .modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<% if policy(@laboratory).edit? %>"+
  '<%= link_to edit_laboratory_path(@laboratory), remote: true, class: "btn btn-warning btn-md" do %>'+
    '<i class="fa fa-edit"></i>'+
    ' Editar laboratorio'+
  '<% end %>'+
  "<% end %>"
);

// Show the dynamic dialog
$('#dialog').modal("show");
