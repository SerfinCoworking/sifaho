// Style modal

// Add the dialog title
$('#dialog .modal-title').html("<i class='fa fa-trash'></i>  Borrando laboratorio");
$('#dialog .modal-header').addClass('bg-danger');
$('#dialog .modal-header').addClass('text-white');

// Render the edit form
$('#dialog .modal-body').html('Esta seguro que desea eliminar el laboratorio <%= @laboratory.name %>?');

// Add form button
$('#dialog .modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<a data-remote='true' data-method='delete' href='/laboratories/<%= @laboratory.id %>>' data-dismiss='modal' class='btn commit btn-danger pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Remove style
$('#dialog').on('hidden.bs.modal', function () {
  $('#dialog .modal-header').removeClass('bg-danger');
  $('#dialog .modal-header').removeClass('text-white');
});
