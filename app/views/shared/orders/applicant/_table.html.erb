<div class="card mx-auto">
  <table class="table table-hover table-sm mb-0">
    <thead>
      <tr>
        <th>Código</th>
        <th>Producto</th>
        <th>Unidad</th>
        <th>Tu stock</th>
        <th>Solicitar</th>
        <th>Tu observación</th>
        <th></th>
      </tr>
    </thead>
    <tbody id="order-product-cocoon-container">
      <%= render "shared/orders/applicant/fields", order: order, order_product: order_product %>
    </tbody>
    
  </table>

  <%= link_to new_external_orders_applicant_product_path(order), class: 'btn btn-primary btn-sm', remote: true do %>
    Agregar producto
    <%= fa_icon "plus" %>
  <% end %>
  
  <%# Si no se asigno algun producto, entonces debemos mostrar mensaje "presence" %>
  <%# if form.object.errors[:presence_of_products_into_the_order].any? %>
    <!--div class="invalid-feedback d-block"-->
      <%#= form.object.errors[:presence_of_products_into_the_order].first %>
    <!--/div-->
  <%# end %>
</div>