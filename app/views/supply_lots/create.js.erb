<%= render 'shared/flash_message' %>

<% if @supply_lot.id.present? %>

  $('#supply_lots').prepend('<%= j render (@supply_lot) %>');

  // Efecto de ingreso
  $('#supply_lot_<%= @supply_lot.id %>').hide().fadeIn(500);

<% end %>
