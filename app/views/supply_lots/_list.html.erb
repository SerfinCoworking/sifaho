<div id="filterrific_results">
  <div class="panel panel-default">
    <table class="table table-hover table-striped">
      <thead>
        <tr>
          <th>N° Lote</th>
          <th><%= filterrific_sorting_link(@filterrific, :codigo) %></th>
          <th><%= filterrific_sorting_link(@filterrific, :insumo) %></th>
          <th><%= filterrific_sorting_link(@filterrific, :estado) %></th>
          <th><%= filterrific_sorting_link(@filterrific, :cantidad_inicial) %></th>
          <th><%= filterrific_sorting_link(@filterrific, :cantidad) %></th>
          <th><%= filterrific_sorting_link(@filterrific, :fecha_recepcion) %></th>
          <th><%= filterrific_sorting_link(@filterrific, :fecha_expiracion) %></th>
          <th colspan="3">Acciones</th>
        </tr>
      </thead>
      <tbody id=supply_lots>
        <%= render @supply_lots %>
      </tbody>
    </table>
  </div>
  <div id="page_links">
    <%= paginate @supply_lot %>
  </div>
</div>
