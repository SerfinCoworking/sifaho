// Style modal
$('.modal-header').addClass('bg-danger text-white');

// Add the dialog title
$('.modal-title').html("<i class=' glyphicon glyphicon-trash'></i>  Eliminar la plantilla <%= @internal_order_template.name %>");

// Render the edit form
$('.modal-body').html(
  'Desea eliminar esta plantilla de <%= @internal_order_template.internal_order_template_supplies.count %> insumos?');

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<a data-remote='true' data-method='delete' href='/internal_order_templates/<%= @internal_order_template.id %>' data-dismiss='modal' class='btn commit btn-danger pull-right' rel='nofollow'>Confirmar</a>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Remove style
$('#dialog').on('hidden.bs.modal', function () {
  $('.modal-header').removeClass('bg-danger text-white');
});
