<div class="">
  <%= notification.actor.full_name %> de <%= notification.actor.sector.establishment.name %>
  <%= notification.action_type %> una <span class="visible-print">solicitud de abastecimiento</span>
  <strong><%= link_to 'solicitud de abastecimiento', main_app.ordering_supply_path(notification.target), class:'hidden-print' %></strong> de 
  <%= osCount = notification.target.quantity_ord_supply_lots.count %><%= ' insumo'.pluralize(osCount) %>
  al sector <%= notification.target.provider_sector.name %> de <%= notification.target.provider_sector.establishment.name %>.
</div>