<p>Quién llevará el pedido?</p>
<%= simple_form_for @internal_order, remote: true, html: {class: 'form-inline'} do |f| %>
  <%= f.error_notification %>
  <%= f.input :sent_by_id,
    collection: @users.map { |user| [user.full_name, user.id] },
    label: false,
    :input_html => {
      required: true,
      class: "selectpicker custom-select-pick show-tick",
      id: "sent-by-select",
      "data-width"=>"300px",
      "title"=>"Seleccionar una persona",
      "data-header"=>"Seleccionar una persona",
      "data-size"=>"10",
      include_blank: false,
      value: "#{ current_user.id }"
    } %>
    <%= f.hidden_field :sent_by_id, class: "sent-by-id" %>
<% end %>
