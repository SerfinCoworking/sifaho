// Add the dialog title
$('#dialog h4').html("<i class='glyphicon glyphicon-plus'></i>  Entregar pedido interno");

// Render the new form
$('.modal-body').html('<%= j render("provider_form") %>');

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<button type='submit' name='commit' class='btn btn-default pull-left' style='margin-left: 30%' form='new_internal_order' value='Cargar'>Cargar</button>"+
  "<button type='submit' name='commit' class='btn btn-primary pull-right' form='new_internal_order' value='Entregar'>Entregar</button>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
  $('#responsable_id_chosen').trigger('chosen:open');
  $('.chosen-single').focus();
});

<%= render 'form_function' %>
